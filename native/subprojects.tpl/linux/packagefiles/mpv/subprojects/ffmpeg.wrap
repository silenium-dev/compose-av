[wrap-file]
directory = ${ffmpeg_directory}
source_url = ${ffmpeg_source_url}
source_filename = ${ffmpeg_source_filename}
source_hash = ${ffmpeg_source_hash}
patch_directory = ffmpeg

[provide]
dependency_names = libavcodec, libavfilter, libavformat, libavutil, libswresample, libswscale
